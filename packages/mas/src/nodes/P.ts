import { TransformNode } from "@hlang-org/runtime";

class PublishAgent extends TransformNode {
  constructor(opt) {
    super(opt);
  }

  _transform($i: any, $o: any): void {
    
  }
}

export default PublishAgent;
